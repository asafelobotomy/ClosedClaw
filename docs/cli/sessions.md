---
summary: "CLI reference for `ClosedClaw sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `ClosedClaw sessions`

List stored conversation sessions.

```bash
ClosedClaw sessions
ClosedClaw sessions --active 120
ClosedClaw sessions --json
```
