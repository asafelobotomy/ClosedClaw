---
summary: "CLI reference for `ClosedClaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `ClosedClaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
ClosedClaw uninstall
ClosedClaw uninstall --all --yes
ClosedClaw uninstall --dry-run
```
